/**
 * VehicleAI — Runtime Configuration
 *
 * LOCAL DEV:  Leave VEHICLEAI_API_URL empty (falls back to localhost:8000)
 * PRODUCTION: Set VEHICLEAI_API_URL to your Render backend URL, e.g.:
 *             window.VEHICLEAI_API_URL = "https://vehicleai.onrender.com";
 *
 * After deploying the backend to Render, paste your Render URL below
 * and redeploy this file to Vercel.
 */
window.VEHICLEAI_API_URL = "";   // ← paste your Render URL here before Vercel deploy
